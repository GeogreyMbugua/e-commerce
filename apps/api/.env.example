NODE_ENV=development
PORT=3001
LOG_LEVEL=info
API_PREFIX=api/v1
DATABASE_URL=postgresql://audiovintage:audiovintage@localhost:5432/audiovintage
# Optional: Render/Neon production connection string for one-off ops from your machine.
# Never commit real credentials. Prefer Render auto-migrate on deploy.
# PRODUCTION_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
REDIS_URL=redis://localhost:6379
CORS_ORIGIN=http://localhost:3000
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Clerk (preferred). When CLERK_SECRET_KEY is set, API verifies Clerk session JWTs.
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
# Optional: comma-separated JWT azp origins, e.g. http://localhost:3000,https://org.github.io
CLERK_AUTHORIZED_PARTIES=
STOREFRONT_URL=http://localhost:3000
STOREFRONT_BASE_PATH=
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=AudioVintage <orders@audiovintage.local>
DEV_JWT_SECRET=audiovintage-local-dev-secret-key-min-16
# Fallback email login when Clerk is unset. Production: set true only if needed.
ALLOW_DEV_AUTH=false
ADMIN_EMAILS=admin@audiovintage.local
UPLOAD_DIR=uploads
PUBLIC_API_URL=http://localhost:3001
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=audiovintage/products
OIDC_ISSUER=
OIDC_AUDIENCE=
OIDC_JWKS_URI=
THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=100
